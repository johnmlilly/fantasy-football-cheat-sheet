export default function Footer() {
     return (
       <p className="text-xs text-stone-400 mt-2 text-center print:hidden">
         Saved to your account — print or screenshot your board before draft
         day.
       </p>
     );
}